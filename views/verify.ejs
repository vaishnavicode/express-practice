<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>Verification</title>
  </head>
  <body>
    <center>
      <form action="/verify" method="POST" name="login" id="login">
        <h2>Verify Your Email</h2>
        <table>
          <% if (send) { %>
          <tr>
            <th><label for="otp">OTP:</label></th>
            <td><input type="text" name="otp" maxlength="6" /></td>
          </tr>

          <% if (errors && errors.otp) { %>
          <tr>
            <td colspan="2" style="color: red">
              <strong><%= errors.otp %></strong>
            </td>
          </tr>
          <% } %>

          <tr>
            <td colspan="2">
              <a href="/resend" class="verification-link">Resend OTP</a>
            </td>
          </tr>
          <% } %>
          <tr>
            <td colspan="2">
              <button type="submit">Verify</button>
            </td>
          </tr>
        </table>
      </form>
    </center>
  </body>
</html>
